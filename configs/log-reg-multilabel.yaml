# configs/log-reg-binary.yaml

usecase: "multilabel"
start_step: "model"
load:
  data_file: "preprocessed_data_lrm.csv"
  features_file: "features_lrm.pkl"
#   model_file: "log_reg_multilabel.pkl"

data_loader:
  promo_file: "data/raw/promotional.csv"
  shuffle: false
  save: "loaded_data_lrm.csv"

preprocessing:
  remove_non_word: true
  convert_lowercase: true
  remove_stopwords: true
  apply_stemming: false
  remove_numbers: false
  remove_whitespace: true
  save: "preprocessed_data_lrm.csv"

features:
  type: tfidf
  ngram_range: [1, 1]
  max_df: 0.9
  min_df: 0.001
  max_features: 10_000
  sublinear_tf: false
  save: "features_lrm.pkl"

model:
  type: logistic_regression
  test_size: 0.2
  random_state: 42
  grid_search: false
  penalty: "l1"
  solver: "liblinear"
  max_iter: 500
  oversampling: false
  save: "log_reg_multilabel.pkl"

evaluation:
  save: "evaluation_lrm.png"
